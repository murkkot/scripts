services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    environment:
      DOMAIN: "https://vw.wirelesscats.keenetic.pro"
    volumes:
      - ./data/:/data/
    ports:
      - 8001:80